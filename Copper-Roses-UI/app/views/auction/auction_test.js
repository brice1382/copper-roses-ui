'use strict';

describe('auction module', function() {

  beforeEach(module('auction'));

  describe('auction controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var auctionCtrl = $controller('AuctionCtrl');
      expect(auctionCtrl).toBeDefined();
    }));

  });
});